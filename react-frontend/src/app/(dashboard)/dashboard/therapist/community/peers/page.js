import PeerDirectoryClient from './PeerDirectoryClient'

export const metadata = {
  title: 'Peer Directory | Community Hub',
  description: 'Find and connect with fellow clinical specialists.',
}

export default function PeerDirectoryPage() {
  return <PeerDirectoryClient />
}
