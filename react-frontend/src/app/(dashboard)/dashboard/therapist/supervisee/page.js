import SuperviseeClient from "./SuperviseeClient";

export const metadata = {
  title: "Supervisee Suite | MLC",
  description: "Workspace for supervisee notes and supervision reflection.",
};

export default function SuperviseePage() {
  return <SuperviseeClient />;
}
